<?php

return [

    /*
    |--------------------------------------------------------------------------
    | API authentication (Sanctum)
    |--------------------------------------------------------------------------
    */
    'api_auth_enabled' => env('API_AUTH_ENABLED', false),

    /*
    |--------------------------------------------------------------------------
    | KPI summary cache (seconds)
    |--------------------------------------------------------------------------
    */
    'summary_cache_seconds' => (int) env('VLAB_SUMMARY_CACHE_SECONDS', 15),

    /*
    |--------------------------------------------------------------------------
    | Paginated list cache (seconds)
    |--------------------------------------------------------------------------
    */
    'list_cache_seconds' => (int) env('VLAB_LIST_CACHE_SECONDS', 30),

];
